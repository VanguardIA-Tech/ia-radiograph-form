# Exemplo de variáveis de ambiente
# Copie para .env e ajuste conforme necessário

# Microsoft Clarity - ID do projeto
# 
# DESENVOLVIMENTO LOCAL (build-time):
# VITE_CLARITY_ID=seu_clarity_project_id_aqui
#
# PRODUÇÃO (runtime no container/EasyPanel):
# CLARITY_ID=seu_clarity_project_id_aqui
# 
# Como funciona:
# 1. Em DEV: npm run dev usa VITE_CLARITY_ID (build-time via Vite)
# 2. Em PROD: Docker injeta window.CLARITY_ID no index.html via script
#    - O Dockerfile busca a variável CLARITY_ID do container
#    - Injeta <script>window.CLARITY_ID='value';</script> antes do bundle
# 3. Prioridade de busca em runtime:
#    a. window.CLARITY_ID (injetado pelo container)
#    b. window.__APP_CONFIG__.clarityId (se existir)
#    c. import.meta.env.VITE_CLARITY_ID (fallback dev)

VITE_CLARITY_ID=
CLARITY_ID=

# Outras variáveis Vite podem ser adicionadas aqui
# VITE_API_URL=
